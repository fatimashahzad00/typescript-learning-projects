/*------------------------------ RETURN TYPES ------------------------------------------------------ */
function hello(name) {
    if (name === void 0) { name = "Alexander"; }
    return name;
}
hello();
