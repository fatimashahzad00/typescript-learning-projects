let imran = {
    name: {firstName: string, lastName: string, middleName: string},
    age: number,
    teacher: boolean,
    dob: Date,
    certifications: [certificatons1, certifications2, ...],
    teacherClass: function() // object has function property which we called method in typescript
}